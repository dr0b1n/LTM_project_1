# RAG-Powered Vendor Compliance Auditor
# Target runtime: CPython 3.12 (64-bit)
# Review and lock exact transitive versions in the approved build environment before production release.

# Web UI and API
streamlit>=1.40,<2
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
python-multipart>=0.0.12,<1
httpx>=0.27,<1

# Configuration, validation, resilience and logging
pydantic>=2.9,<3
pydantic-settings>=2.5,<3
python-dotenv>=1.0,<2
tenacity>=9,<10
structlog>=24,<26
orjson>=3.10,<4

# Document ingestion
pypdf>=5,<7
PyMuPDF>=1.24,<2
python-docx>=1.1,<2
filetype>=1.2,<2

# PII detection and anonymization
presidio-analyzer>=2.2,<3
presidio-anonymizer>=2.2,<3
spacy==3.8.16

# Compatible CLI dependency set for spaCy and Instructor
instructor>=1.13,<2
typer>=0.27,<1
click>=8.4.2,<9

# Embeddings and retrieval
# Embeddings are generated by the local Ollama runtime using granite-embedding:30m.
# Verified embedding dimension: 384.
numpy>=1.26,<3
chromadb>=1.0,<2

# Local LLM client (Ollama is an external runtime)
ollama>=0.4,<1

# Persistence and migrations
SQLAlchemy>=2.0,<3
alembic>=1.13,<2

# Authentication and security helpers
PyJWT[crypto]>=2.9,<3
pwdlib[argon2]>=0.2,<1
cryptography>=43,<49

# Metrics and health monitoring
prometheus-client>=0.21,<1

# RAG evaluation and automated QA
ragas>=0.2,<1
pytest>=8,<10
pytest-asyncio>=0.24,<2
pytest-cov>=6,<8

# Code quality and security scanning
ruff>=0.8,<1
mypy>=1.13,<2
pip-audit>=2.7,<3
bandit>=1.8,<2

# External runtime/model downloads not installed by this file:
# 1. python -m spacy download en_core_web_lg
# 2. Ollama runtime
# 3. Ollama embedding model: granite-embedding:30m
# 4. Approved Ollama chat model, such as granite4.1:8b
